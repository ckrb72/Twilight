#include "Twilight.h"
namespace Twilight
{
    Twilight::Twilight()
    {
    
    }
    
    Twilight::~Twilight()
    {
    
    }
    
    void Twilight::init()
    {
        /*
            window.init(Twilight::Window::FULLSCREEN);
            renderer.init(Twilight::Renderer::VULKAN);
            event_manager.init();
            physics_world.init();
            asset_manager.init();
        */
    }
    
    void Twilight::deinit()
    {
        /*
            asset_manager.deinit();
            physics_world.deinit();
            event_manager.deinit();
            renderer.deinit();
            window.deinit();
        */
    }
}